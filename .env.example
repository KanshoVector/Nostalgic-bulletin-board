# Docker Compose 専用（ローカル検証時のみ使用）
# cp .env.example .env
#
# 大学サーバーへアップロードする PHP ファイルは .env 不要。
# 環境変数が未設定の場合、includes/db_connection.php が大学 DB に自動接続します。

POSTGRES_DB=board_db
POSTGRES_USER=board_user
POSTGRES_PASSWORD=board_pass
APP_PORT=8080
